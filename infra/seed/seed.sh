#!/bin/bash
# Simple shell script wrapper for seeding if needed, though we'll use TS script primarily
echo "Seeding database..."
# This is a placeholder if we want to run raw psql commands
# psql $DATABASE_URL -f infra/seed/seed.sql
