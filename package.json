{
  "name": "caceasy-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces",
    "dev": "npm run dev --workspaces"
  },
  "devDependencies": {
    "@types/recharts": "^1.8.29",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "recharts": "^3.5.0"
  }
}
